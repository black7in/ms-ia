services:
  ms-ia:
    build:
      context: .
      target: production
    ports:
      - "8002:8002"
    env_file:
      - .env
    volumes:
      - ./app:/app/app
      - ./tmp:/app/tmp
    restart: unless-stopped
